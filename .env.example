# true/false — CLI e launchStealthBrowser leem HEADLESS se headless não for passado
HEADLESS=true

# Proxy opcional (IP residencial ajuda quando o stealth sozinho não basta)
PROXY_SERVER=http://127.0.0.1:8080
PROXY_USERNAME=
PROXY_PASSWORD=
